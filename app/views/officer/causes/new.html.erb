<% provide(:title, 'Officer: New Cause') %>
<h1>New Cause</h1>

<%= form_for [:officer, @issue, @cause] do |f| %>
	<%= render 'fields', f: f %>
	<%= f.submit "Add Cause", class: "btn btn-large btn-primary" %>
<% end %>